const menu=document.querySelector('.menu');const nav=document.querySelector('#primary-nav');if(menu&&nav){menu.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')==='true';menu.setAttribute('aria-expanded',String(!open));menu.textContent=open?'Menu':'Close';nav.classList.toggle('open',!open)});nav.querySelectorAll('a').forEach((link)=>link.addEventListener('click',()=>{menu.setAttribute('aria-expanded','false');menu.textContent='Menu';nav.classList.remove('open')}))}const observer=new IntersectionObserver((entries)=>entries.forEach((entry)=>{if(entry.isIntersecting)entry.target.classList.add('is-visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach((el)=>observer.observe(el));const form=document.querySelector('[data-form]');if(form)form.addEventListener('submit',(e)=>{e.preventDefault();const s=form.querySelector('[data-status]');if(!form.checkValidity()){s.textContent='Please complete the project fields and add a valid email.';s.className='status error';form.querySelector(':invalid')?.focus();return}s.textContent='Demo state: intake captured. Connect this form to the client endpoint.';s.className='status success'});